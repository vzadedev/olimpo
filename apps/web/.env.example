# Copie para .env.local (desenvolvimento) ou configure no painel da Vercel (produção).
#
# Nota: o frontend é Next.js — variáveis expostas ao browser usam o prefixo NEXT_PUBLIC_.

NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_GRAPHQL_URL=
NEXT_PUBLIC_APP_NAME=OLIMPO
